#Got a new script! Snagged this bad boy from
#https://github.com/pestrada/android-tdd-playground/blob/master/.travis.yml
language: android
jdk: oraclejdk11
sudo: false

android:
 components:
   #- platform-tools
   #- tools
   #- build-tools-30.0.2
   #- android-31
   - android-22
   - sys-img-armeabi-v7a-android-22
   - sys-img-
   - extra-android-m2repository
 licenses:
   - 'android-sdk-preview-license-.+'
   - 'android-sdk-license-.+'
   - 'google-gdk-license-.+'

install:
  - echo y | android update sdk -u -a -t tools
  - echo y | android update sdk -u -a -t platform-tools
  - echo y | android update sdk -u -a -t build-tools-30.0.2
  - echo y | android update sdk -u -a -t android-31

before_script:
 # Create and start emulator
 - echo no | android create avd --force -n test -t android-22 --abi armeabi-v7a
 - emulator -avd test -no-skin -no-audio -no-window &
 - android-wait-for-emulator
 - adb shell input keyevent 82 &
script: ./gradlew connectedAndroidTest